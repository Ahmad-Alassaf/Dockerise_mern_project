version: '3'
services:
  backend:
    container_name: backendContainer
   
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
     
  frontend:
    container_name: frontendContainer
     
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
     
